import Buttons from './Buttons';

const AnimalCards = () => {
    return (
        <div>
            Testing Animal cards
            <Buttons></Buttons>
        </div>


)
};

export default AnimalCards;